{
  "name": "my-project",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "lint": "eslint .",
    "test": "jest --reporters=default --reporters=jest-allure",
    "test:allure": "jest --reporters=jest-allure && allure generate allure-results && allure open",
    "start": "node src/server.js"
  },
  "dependencies": {
    "axios": "^1.3.4",
    "express": "^4.18.2",
    "supertest": "^6.3.0"
  },
  "devDependencies": {
    "allure-commandline": "^2.36.0",
    "eslint": "^8.0.0",
    "jest": "^29.0.0",
    "jest-allure": "^1.0.0"
  },
  "jest": {
    "reporters": [
      "default",
      ["jest-allure", { "resultsDir": "allure-results" }]
    ]
  },
  "engines": {
    "node": ">=22.19.0"
  },
  "files": [
    ".gitignore",
    "README.md",
    "src/index.js",
    "tests/unit/hello.test.js",
    "tests/integration/hello.integration.test.js",
    "tests/e2e/hello.e2e.test.js"
  ]
}
